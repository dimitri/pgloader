# MySQL option file for the my-cnf integration test (issue #767).
#
# Placed at ~/.my.cnf in the test-runner container by the Dockerfile.
# The [client] section uses a wrong user to confirm that [pgloader] wins.

[client]
user = nobody

[pgloader]
user = root
password = rootpassword
